/**
 * React-free icon export names.
 *
 * This source is replaced at build time with the names exported by the icon
 * category indexes. It deliberately has no dependency on icon components.
 */
export const iconNames: readonly string[] = [];

export default iconNames;
